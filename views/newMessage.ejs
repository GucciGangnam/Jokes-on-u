<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel='stylesheet' href='../stylesheets/style.css' />
    <title>Home</title>
</head>

<body>
    <div class="NavBar">
        <% if (user) { %>
            <p>Hello <%= user.USERNAME %>
            </p>
            <% } %>
                <a class="NavButtonLink" href="/">Home</a>
                <% if (!user) { %>
                    <a class="NavButtonLink" href="/signup">Sign Up</a>
                    <% } %>
                        <% if (user) { %>
                            <a class="NavButtonLink" href="/logout">Log out</a>
                            <% } else { %>
                                <a class="NavButtonLink" href="/login">Log in</a>
                                <% } %>
                                    <a class="NavButtonLink" href="/users">Users</a>
    </div>
    <h1> new mesage form </h1>
    <form action="/message" method="post">
        <label for="title">
            Title
        </label>
        <input type="text" name="title" id="title" required>
        </input>
        <label for="message">
            Message
        </label>
        <input type="text" name="message" id="message" required>
        </input>
        <button type="submit">Post</button>
    </form>
</body>

</html>